export enum WsSubscriptionsEnum {
    PING = '[Client] Ping',
    REGISTER = '[Client] Register',
    LOGIN = '[Client] Login',
    CHAT = '[Client] Chat',
    GET_GAMESTATE = '[Client] Get GameState',
}
